(function(global){
  "use strict";

  const runtime=global.AIBA&&global.AIBA.runtime;
  if(!runtime)throw new Error("AIBA legacy adapter requires the AIBA runtime");

  runtime.attachLegacy({
    $,
    G,
    PAUSE,
    DIFFS,
    SCENE_PRESETS,
    LEGENDS,
    TALK_PRE,
    COVER_STARS,
    EXT_AUDIO,
    BATTLE_BAR_VISIBLE_SHOTS,
    BATTLE_TARGET,
    BATTLE_SPOTS,
    BATTLE_NORMAL_STOCK,
    BATTLE_NORMAL_RELOAD,
    BATTLE_DEEP_RELOAD,
    OPP_SPOT_EMPTY_WAIT,
    RACK_RUSH_LEVELS,
    RACK_RUSH_RUNS_KEY,
    RACK_RUSH_SPEED_RUNS_KEY,
    RACK_RUSH_SPEED_TARGET,
    VISION,
    RACKS,
    DEEPS,
    HOOP,
    COURT,
    COURT_ATTACK_DIR,
    GAME_VERSION,
    GAME_NAME,
    GAME_SEED,
    scene,
    balls,
    rivals,
    deepBalls,
    player,
    passer,
    passerBall,
    oppPasser,
    oppPasserBall,
    handBall,
    pBall,
    hands,
    confPts,
    ballGeo,
    matGold,
    matDeep,
    matBall,
    blobGeo,
    blobMat,
    CAM,
    P,
    rig,
    camTarget,
    TILT,
    V3,
    seededRandom,
    buildSeq,
    benchSetup,
    startAIShow,
    stars,
    aiProb,
    shotProfileFor,
    shotFlightTime,
    rnd,
    clamp,
    ease01,
    shotCurves,
    SHOT_STANCE_YAW,
    shotStanceBlend,
    tuneGuideHandPose,
    poseHandJoints,
    captureShotPose,
    applyShotSetPose,
    applyHandFollowThroughPose,
    poseGuy,
    poseBallPos,
    dressGuy,
    applyStarStyle:(...args)=>global.applyStarStyle(...args),
    shotProfileText,
    playerShotProfile,
    resetFinalRun,
    resetAudioCueMemory,
    resetRackBalls,
    stopCelebrate,
    ensureAudio,
    applyScenePreset,
    hidePanel:(...args)=>global.hidePanel(...args),
    music,
    resetProgressiveSceneForRun,
    enterArenaAudio,
    leaveArenaAudio,
    faceTo,
    curShot,
    shotBase,
    walkTo,
    applyCamMode,
    shotEye,
    autoFrameCam,
    glideTo,
    startPreGameShow,
    broadcastSting,
    paSay,
    playPregameCountdownCue,
    sGo,
    calibrateTilt,
    readyBall,
    toast:(...args)=>global.toast(...args),
    sBeep,
    updDotsUI,
    updPowerUI,
    updTargetUI,
    updJumbo,
    playAudioEvent,
    triggerStreetCrowdReaction,
    bloomOnScore,
    beginFinalAudioWindow,
    gameDjSay,
    battleScoreCallout,
    checkBattleOvertake,
    crowdSwell,
    doRelease,
    sBuzz,
    startConfetti,
    cheerSound,
    boo,
    sBounce,
    sSwish,
    popScore:(...args)=>global.popScore(...args),
    airhorn,
    djSay,
    endHero,
    organCharge,
    scoreQuoteMarkup,
    showPanel:(...args)=>global.showPanel(...args),
    startVictoryCine,
    stopVictoryCine,
    startWinCine,
    checkBallCollisions,
    showMenu:(...args)=>global.showMenu(...args),
    goDiff:(...args)=>global.goDiff(...args),
    hasAudioContext:()=>!!AC,
    getPassing:()=>passing,
    setPassing:value=>{passing=value;},
    setCamSnap:value=>{camSnap=!!value;},
    getLowBeep:()=>lowBeep,
    setLowBeep:value=>{lowBeep=value;},
    getScenePreset:()=>currentScenePreset,
    getWeather:()=>currentWeather,
    getSharedRackRush:()=>SHARED_RACK_RUSH,
    pickDiff:(...args)=>global.pickDiff(...args),
    getCurSpotRing:()=>curSpotRing,
    getWinCine:()=>winCine,
    rep,
    refreshBench:()=>benchVis(),
    getJumboAcc:()=>jumboAcc,
    setJumboAcc:value=>{jumboAcc=value;},
    setNetPulse:value=>{netPulse=value;}
  });
})(window);
